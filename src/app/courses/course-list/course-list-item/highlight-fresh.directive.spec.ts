import { HighlightFreshDirective } from './highlight-fresh.directive';

describe('HighlightFreshDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFreshDirective();
    expect(directive).toBeTruthy();
  });
});
